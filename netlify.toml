[build]
  # base = "./"
  publish = "dist"
  command = "rm -rf ./src/gfx/cover/* && npm run build"
  functions = "api-build"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
