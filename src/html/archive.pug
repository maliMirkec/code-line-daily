extends /_layout/layout.pug

block variables
  - pageTitle = "Archive"
  - pageURL = "archive"

block content
  article.w.w--beta.mt--alpha
    h1= "An archive of lines of code of the day."
  .w.w--alpha.mt--alpha
    p.t-sm.t-r= "Sorted by: Date DESC"
  .w.w--alpha.grid
    each listValue, listIndex in listArchive
      +cld(listValue, 'cld--alpha')
